version: 2

# required for altair.plot >=3.11
build:
  os: ubuntu-lts-latest
  tools:
    python: "3.11"

python:
  install:
    - requirements: docs/requirements.txt
#    - method: pip
#      path: .

sphinx:
  configuration: docs/source/conf.py
